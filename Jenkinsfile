// Jenkinsfile (Declarative Pipeline)
import hudson.util.RemotingDiagnostics
import jenkins.model.Jenkins
pipeline {
  agent any
  stages {
    stage('Stage 1') {
      steps {
        echo 'Hello world!'
      }
    }
  }
}
